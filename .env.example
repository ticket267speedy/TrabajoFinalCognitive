# Template only - NEVER commit with real credentials

# Supabase PostgreSQL Connection (REQUIRED)
# Get from: Supabase Dashboard → Settings → Database → Connection String (URI)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_ID:YOUR_PASSWORD@aws-1-sa-east-1.pooler.supabase.com:5432/postgres

# Security - Generate new keys in production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-here

# Environment
FLASK_ENV=development
FLASK_APP=run.py
